export class Activity {
    constructor(data) {
        this.day = 2
        // this.userInfos ={}
        // if(data && data.userInfos && data.userInfos.firstName) this.userInfos.firstName = data.userInfos.firstName
        // else this.userInfos.firstName = ""
        // if(data && data.userInfos && data.userInfos.lastName) this.userInfos.lastName = data.userInfos.lastName
        // else this.userInfos.lastName = ""
        // if(data && data.userInfos && data.userInfos.age) this.userInfos.age = data.userInfos.age
        // else this.userInfos.age = 0

        // if(data && data.todayScore) this.todayScore = data.todayScore
        // else this.todayScore = 1

        // this.keyData = {}
        // if(data && data.keyData && data.keyData.calorieCount) this.keyData.calorieCount = data.keyData.calorieCount
        // else this.keyData.calorieCount = 0
        // if(data && data.keyData && data.keyData.proteinCount) this.keyData.proteinCount = data.keyData.proteinCount
        // else this.keyData.proteinCount = 0
        // if(data && data.keyData && data.keyData.carbohydrateCount) this.keyData.carbohydrateCount = data.keyData.carbohydrateCount
        // else this.keyData.carbohydrateCount = 0
        // if(data && data.keyData && data.keyData.lipidCount) this.keyData.lipidCount = data.keyData.lipidCount
        // else this.keyData.lipidCount = 0
    }
}
